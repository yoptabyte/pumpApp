version: "3.8"

services:
  db:
    image: postgres:17-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${DB_NAME:-pampdb}
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-pampdb}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app_network

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - app_network

  minio:
    image: minio/minio:RELEASE.2025-02-28T09-55-16Z
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${S3_ACCESS_KEY_ID}
      - MINIO_ROOT_PASSWORD=${S3_SECRET_ACCESS_KEY}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - app_network

  minio_init:
    image: minio/mc:RELEASE.2025-02-21T16-00-46Z
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 ${S3_ACCESS_KEY_ID} ${S3_SECRET_ACCESS_KEY}; do sleep 2; done &&
      mc mb -p local/${S3_MEDIA_BUCKET:-media} || true &&
      mc anonymous set none local/${S3_MEDIA_BUCKET:-media}
      "
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - app_network

  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - static_volume:/app/static
      - media_volume:/app/media
      - frontend_build:/app/frontend/build
      - /app/frontend/node_modules
    ports:
      - "8000:8000"
      - "3001:3000"
    environment:
      - DEBUG=${DEBUG}
      - SECRET_KEY=${SECRET_KEY}
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1],web,pumpapp.com
      - CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:8000,http://localhost:8080,http://127.0.0.1:3001,http://127.0.0.1:8000,http://127.0.0.1:8080,http://web:3000,http://web:8000
      - CSRF_TRUSTED_ORIGINS=http://localhost:3001,http://localhost:8000,http://localhost:8080,http://127.0.0.1:3001,http://127.0.0.1:8000,http://127.0.0.1:8080,http://web:3000,http://web:8000
      - DB_NAME=${DB_NAME:-pampdb}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-5432}
      - REDIS_URL=redis://redis:6379/1
      - BASE_API_URL=http://localhost:8000
      - BASE_APP_URL=http://localhost:3001
      - FRONTEND_URL=http://localhost:3001
      - VITE_API_URL=http://localhost:8000/api/v1/
      - GOOGLE_OAUTH2_KEY=${GOOGLE_OAUTH2_KEY}
      - GOOGLE_OAUTH2_SECRET=${GOOGLE_OAUTH2_SECRET}
      - NODE_ENV=development
      - SERVICE_NAME=web
      - PORT=3000
      - WDS_SOCKET_PORT=3000
      - CHOKIDAR_USEPOLLING=true
      - COOKIE_SECURE=${COOKIE_SECURE:-0}
      - COOKIE_SAMESITE=${COOKIE_SAMESITE:-Lax}
      - SECURE_SSL_REDIRECT=${SECURE_SSL_REDIRECT:-0}
      - USE_S3_MEDIA_STORAGE=${USE_S3_MEDIA_STORAGE:-1}
      - S3_ENDPOINT_URL=${S3_ENDPOINT_URL:-http://minio:9000}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY}
      - S3_MEDIA_BUCKET=${S3_MEDIA_BUCKET:-media}
      - S3_MEDIA_BASE_URL=${S3_MEDIA_BASE_URL:-http://localhost:9000/media}
      - S3_REGION=${S3_REGION:-us-east-1}
      - S3_ADDRESSING_STYLE=${S3_ADDRESSING_STYLE:-path}
      - S3_SIGNATURE_VERSION=${S3_SIGNATURE_VERSION:-s3v4}
      - S3_QUERYSTRING_AUTH=${S3_QUERYSTRING_AUTH:-1}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio_init:
        condition: service_completed_successfully
    networks:
      - app_network
    restart: unless-stopped

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    entrypoint: /app/bot_entrypoint.sh
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - BOT_API_KEY=${BOT_API_KEY}
      - API_BASE_URL=http://web:8000
      - TIMEZONE=Europe/Lisbon
      - PYTHONPATH=/app
      - SERVICE_NAME=bot
      - HEADERS_HOST=web
      - BOT_LOG_LEVEL=${BOT_LOG_LEVEL:-WARNING}
    depends_on:
      web:
        condition: service_healthy
    networks:
      - app_network
    restart: unless-stopped

  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - static_volume:/app/static
      - media_volume:/app/media
    environment:
      - DEBUG=${DEBUG}
      - SECRET_KEY=${SECRET_KEY}
      - DB_NAME=${DB_NAME:-pampdb}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-5432}
      - REDIS_URL=redis://redis:6379/1
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - SERVICE_NAME=celery_worker
      - USE_S3_MEDIA_STORAGE=${USE_S3_MEDIA_STORAGE:-1}
      - S3_ENDPOINT_URL=${S3_ENDPOINT_URL:-http://minio:9000}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY}
      - S3_MEDIA_BUCKET=${S3_MEDIA_BUCKET:-media}
      - S3_MEDIA_BASE_URL=${S3_MEDIA_BASE_URL:-http://localhost:9000/media}
      - S3_REGION=${S3_REGION:-us-east-1}
      - S3_ADDRESSING_STYLE=${S3_ADDRESSING_STYLE:-path}
      - S3_SIGNATURE_VERSION=${S3_SIGNATURE_VERSION:-s3v4}
      - S3_QUERYSTRING_AUTH=${S3_QUERYSTRING_AUTH:-1}
    depends_on:
      web:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio_init:
        condition: service_completed_successfully
    networks:
      - app_network
    restart: unless-stopped

  celery_beat:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - static_volume:/app/static
      - media_volume:/app/media
    environment:
      - DEBUG=${DEBUG}
      - SECRET_KEY=${SECRET_KEY}
      - DB_NAME=${DB_NAME:-pampdb}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-5432}
      - REDIS_URL=redis://redis:6379/1
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - SERVICE_NAME=celery_beat
      - USE_S3_MEDIA_STORAGE=${USE_S3_MEDIA_STORAGE:-1}
      - S3_ENDPOINT_URL=${S3_ENDPOINT_URL:-http://minio:9000}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY}
      - S3_MEDIA_BUCKET=${S3_MEDIA_BUCKET:-media}
      - S3_MEDIA_BASE_URL=${S3_MEDIA_BASE_URL:-http://localhost:9000/media}
      - S3_REGION=${S3_REGION:-us-east-1}
      - S3_ADDRESSING_STYLE=${S3_ADDRESSING_STYLE:-path}
      - S3_SIGNATURE_VERSION=${S3_SIGNATURE_VERSION:-s3v4}
      - S3_QUERYSTRING_AUTH=${S3_QUERYSTRING_AUTH:-1}
    depends_on:
      web:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio_init:
        condition: service_completed_successfully
    networks:
      - app_network
    restart: unless-stopped

  nginx:
    image: nginx:1.25-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - static_volume:/app/static:ro
      - media_volume:/app/media:ro
      - frontend_build:/app/frontend/build:ro
    ports:
      - "8080:8080"
    depends_on:
      web:
        condition: service_healthy
    networks:
      - app_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  app_network:
    name: pamp_network
    driver: bridge

volumes:
  postgres_data:
    name: pamp_postgres_data
  static_volume:
    name: pamp_static_volume
  media_volume:
    name: pamp_media_volume
  frontend_build:
    name: pamp_frontend_build
  redis_data:
    name: pamp_redis_data
  minio_data:
    name: pamp_minio_data
